How to compile.

```
# In container

cd ~/project/09/HelloWorld
ln -s ~/tools/OS/* .
bash ~/tools/JackCompiler.sh .

bash ~/tools/VMEmulator.sh # open `~/project/09/HelloWorld` directory, not files.
```
